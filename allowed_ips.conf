# IP Whitelist — one entry per line.
# Comments with #. Blank lines ignored.
# Supported: single IPv4, IPv4 CIDR, single IPv6, IPv6 CIDR.
# 127.0.0.1 and ::1 are always allowed unconditionally.

127.0.0.1
::1
# 10.10.0.0/16        # office subnet
# 192.168.100.50      # VPN endpoint
